#include "pch.h"
#include "vrLog.h"
#include <string>
#include <sstream>
#include <Windows.h>
int ik_test(int theta)
{
	//std::stringstream info;
	//info << theta;
	//MessageBoxA(NULL, info.str().c_str(), NULL, MB_OK);
	return ++ theta;
}